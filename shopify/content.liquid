<!-- theme.liquid -->

{{ content_for_layout }}

// This tag needs to be between the opening and closing body tag.
// It gets replaced by the dynamic content generated by whichever template is being used.


<hr />


<!-- Featured product -->

// Output three products from the collection called "frontpage"
<div id="featured">
    <h2>Featured Products</h2>
    {% for product in collections.frontpage.products limit:3  %}
        <a href="{{product.url}}">{{ product.featured_image | product_img_url: 'medium' | img_tag }}</a>
        <h3><a href="{{product.url}}">{{product.title}}</a></h3>
        <ul class="attributes">
            <li><span class="money">{{product.price_min | money}}</span></li>
        </ul>
    {% endfor %}
</div>


<hr />


<!-- Display editable text -->

// Output the text from the page with a handle of "frontpage". This allows you to offer simple editing for home page content.
{% assign article = pages.frontpage %}
{% if article.content != "" %}  
<div id="frontpage-article">
    <div class="article">
        <h3>{{ article.title }}</h3>
        <div class="article-body textile">{{ article.content }}</div>
    </div>
</div>
{% endif %}